defmodule CheckersWeb.MatchView do
  use CheckersWeb, :view

  # alias Checkers.Games
  # alias Checkers.Accounts

  # def get_usernames(%Games.Match{white_id: white_id, black_id: black_id}) do
  #   white = Accounts.get_user!(white_id)
  #   black = Accounts.get_user!(black_id)
  # end
end
